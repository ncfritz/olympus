# Copy to local.env (all git-ignored) and fill in.
# `pnpm dev:local` reads it.
#
# local.env (`pnpm dev:local`) runs this service from the IDE on the laptop,
# against the laptop's own stack (infra/docker, env/laptop.env): Hasura on
# localhost:8080 and RabbitMQ's /dionysus-dev vhost as the olympus-dev
# user, whose password is the laptop's SECRETS_DIR/rabbitmq/olympus-dev.password.

LISTEN_PORT=3104

# Logging
LOKI_URL=
LOKI_LEVEL=info
LOKI_LOGGING_LEVEL=info
CONSOLE_LOGGING_LEVEL=info
# ENABLE_CONSOLE_LOGGING=true
# FILE_LOGGING_ENABLED=false
# FILE_LOGGING_LEVEL=info
# FILE_LOGGING_PATH=./logs

# RabbitMQ
AMQP_PROTOCOL=amqp
AMQP_HOST=localhost
AMQP_PORT=5672
AMQP_USER=olympus-dev
AMQP_PASSWORD=
AMQP_VHOST=/dionysus-dev

# Olympus
API_BASE_URL=http://localhost:3001/v1
# The API's mTLS listener (ADR 0018). `scripts/dev-ca.sh` writes a
# throwaway certificate for this agent.
# API_BASE_URL=https://localhost:3443/v1
# API_CLIENT_CERT=../../infra/dev-ca/certs/agents/dionysus-search-agent.crt
# API_CLIENT_KEY=../../infra/dev-ca/certs/agents/dionysus-search-agent.key
# API_CA_CERT=../../infra/dev-ca/certs/services-ca.crt

# NzbGeek
# NZBGEEK_API_URL=https://api.nzbgeek.info/api
NZBGEEK_API_KEY=
