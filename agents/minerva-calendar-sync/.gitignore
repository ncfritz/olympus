# SQLite databases (DATABASE_URL="file:./dev.db") and test leftovers
*.sqlite
*.sqlite-journal
agent/*.db
agent/*.db-journal
agent/prisma/*.db
agent/prisma/*.db-journal

# OAuth credentials stored by the calendar connectors
agent/.credentials/
agent/.credentials-microsoft/
