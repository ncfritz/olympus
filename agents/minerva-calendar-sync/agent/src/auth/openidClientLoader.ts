/**
 * Moved to @ncfritz/olympus-nest, where the API can reach the same
 * workaround. The comment there explains why `new Function` is necessary.
 */
export { loadOpenIdClient } from "@ncfritz/olympus-nest";
