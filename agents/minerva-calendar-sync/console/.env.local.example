# The agent, which runs on its own port in the workspace. In the image
# the console and its agent share an origin and this is left unset: the
# console asks for its own agent at /minerva/calendar/api (ADR 0021).
NEXT_PUBLIC_API_URL="http://localhost:4432"

# The rest of the suite, so the sidebar's other consoles reach the home
# lab rather than 404ing on localhost. Unset is fine.
# CONTROL_ORIGIN="https://control.olympus.internal.ncfritz.net"
