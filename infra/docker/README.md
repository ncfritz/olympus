# Docker

Not yet populated. Planned contents (ADR 0007):

- `docker-bake.hcl`: every image, its target platform(s) and tags
- `compose/mac-mini.yml`, `compose/synology.yml`: per-host deployments
- Dockerfiles stay next to each app; they use `turbo prune <app> --docker`
