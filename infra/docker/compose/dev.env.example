# Development only: these values never leave the machine.
POSTGRES_USER=olympus
POSTGRES_PASSWORD=olympus
POSTGRES_DB=olympus
POSTGRES_PORT=55432
HASURA_PORT=8080
HASURA_GRAPHQL_ADMIN_SECRET=olympus-dev
# The engine version the platform runs; keep it in step with the Mac Mini.
HASURA_VERSION=v2.39.1
