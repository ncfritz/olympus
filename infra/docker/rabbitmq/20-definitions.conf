# Users, vhosts and permissions come from a definitions file that
# infra/docker/rabbitmq/definitions.mjs writes into the host's secrets
# directory; Compose mounts it as a secret (ADR 0019).
definitions.import_backend = local_filesystem
definitions.local.path = /run/secrets/rabbitmq_definitions
